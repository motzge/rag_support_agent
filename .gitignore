# ── Python ──
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.pytest_cache/

# ── Virtual environment ──
venv/
.venv/

# ── Secrets / local config ──
.env
.env.*
!.env.example
.streamlit/secrets.toml

# ── Vector store (rebuilt via ingest/loader.py) ──
data/chroma/
data/chroma_python_backup/

# ── Runtime artifacts ──
data/memory.db
data/agent.log
*.log

# ── Downloaded docs (rebuilt by the loader) ──
data/docs/

# ── OS / editor ──
.DS_Store
*.swp
.idea/
.vscode/