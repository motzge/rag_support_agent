# Copy this file to .env and adjust if you use different local models.
# Both models must be pulled in Ollama first (ollama pull <model>).

OLLAMA_MODEL=qwen2.5:14b
OLLAMA_EMBED_MODEL=nomic-embed-text